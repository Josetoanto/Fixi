import { PreviuwFilePipe } from './previuw-file.pipe';

describe('PreviuwFilePipe', () => {
  it('create an instance', () => {
    const pipe = new PreviuwFilePipe();
    expect(pipe).toBeTruthy();
  });
});
